is-installed() {
	which pv &> /dev/null
}

install() {
	test -d pv-1.6.6 || curl -L http://www.ivarch.com/programs/sources/pv-1.6.6.tar.gz | tar -xz
	pushd pv-1.6.6
	./configure --prefix=$PREFIX
	make install
	popd
}
